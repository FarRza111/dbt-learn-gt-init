sources:
  - name: jaffle_shop
    description: A cloe of a Postgress database
    database: raw
    schema: jaffle_shop
    tables:
      - name: customers
        description: Raw customers data from databricks

      - name: orders
        description: Raw orders data from databricks
        config:
          freshness:
            warn_after:
              count: 8
              period: hour
            error_after:
              count: 10
              period: minute

        # loaded_at_field: _etl_loaded_at 
    

